
/* DOUBLE FUNCTIONS */
/* char_to_double - takes a char as parameter and returns its cast in double. */
extern double char_to_double(char a)
{
    return (double) a;
}

/* int_to_double - takes a int as parameter and returns its cast in double. */
extern double int_to_double(int a)
{
    return (double) a;
}

/* void_to_double - takes a void as parameter and returns 0. */
extern double void_to_double(void)
{
    return 0;
}


/* INT FUNCTIONS */
/* char_to_double - takes a char as parameter and returns its cast in double. */
extern int char_to_int(char a)
{
    return (int) a;
}

/* double_to_int - takes a double as parameter and returns its cast in int. */
extern int double_to_int(double a)
{
    return (int) a;
}

/* void_to_int - takes a void as parameter and returns 0. */
extern int void_to_int(void)
{
    return 0;
}


/* CHAR FUNCTIONS */
/* double_to_char - takes a double as parameter and returns its cast in char. */
extern char double_to_char(double a)
{
    return (char) a;
}

/* int_to_char - takes a int as parameter and returns its cast in char. */
extern char int_to_char(int a)
{
    return (char) a;
}

/* void_to_char - takes a void as parameter and returns 0. */
extern char void_to_char(void)
{
    return 0;
}

